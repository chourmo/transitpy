name: development
channels:
  - conda-forge
  - defaults
dependencies:
    # base
  - python>=3.9
  - pip
  - aiohttp
  - numpy
  - scipy
  - pandas
  - geopandas
  - pygeos
  - streetpy

    # developement
  - ipykernel
  - black

    # testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

